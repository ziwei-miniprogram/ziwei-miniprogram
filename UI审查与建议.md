# 星野漫游 · UI 完整性与体验审查（像素君）

> 视角：UI Designer。不仅看"能不能跑"，更看视觉一致性、无障碍、组件复用与"能跑但体验会翻车"的隐患。

## 一、完整性结论 ✅

| 维度 | 结果 |
|------|------|
| 页面结构 | 14 个页面 + 自定义 tabBar + 6 个工具模块 + 1 个云函数，结构完整 |
| JS 语法 | 26 个 `.js` 全部 `node --check` 通过 |
| 导航死链 | 无。所有 `navigateTo`/`switchTab` 目标均在 `app.json` 注册 |
| tabBar 契约 | 索引对齐正确（4 真 tab 的 `selected` 值与自定义列表 `tabIndex` 一致）；中间"记一笔"走 `navigateTo` |
| 空值保护 | 所有 `getTabBar()` 调用均有 `if (getTabBar())` 守卫，非 tab 页不会崩 |
| 数据绑定 | `chart.cost`、`merit` 进度、`whimsy` 浮层 import/template 均一致 |

**结论：项目不是"一堆 bug"的状态，是可上线打磨的完成度。**

## 二、发现的隐患（按严重度）

### 🟡 轻微 · 品牌一致性
1. **blessing.js 婚礼模板**原用旧紫渐变 `#a18cd1,#fbc2eb`，与金系冲突 → 已顺手改为 `#c8a35a,#e6c9a8`（金→沙，喜庆且不破品牌）。其余生日/新年/寄愿模板是有意的节庆多色，保留。

### 🟠 中 · 无障碍（WCAG AA 未达标）
2. **金色 `#c8a35a` 作文字色**在浅底（`#f7f5f0`/白）上对比度约 **2.5:1**，未达 AA 标准（正文 4.5:1、大字 3:1 均不达标）。影响：`merit` 大数字(80rpx)、各页标题、`act.on` 标签、`p-go` 按钮等。可读性是真问题，尤其户外/老人场景。

### 🟡 轻微 · 调色板治理
3. 出现少量绿 `#5e9788 / #9FD0C2`（跃升/成功语义）。建议正式定为语义色 `--color-success` 并写入 `app.wxss` 令牌，避免散落。

### 🟡 轻微 · 仓库技术债
4. 11 个 `preview-*.html` + 多份 `overview/方案` 文档混在代码目录。建议挪到 `docs/` 与 `previews/`，保持小程序目录干净（这些不影响编译，但碍眼）。

## 三、好玩的建议（待协商，不急着做）

> 原则：在现有「星辉为光，善意有回响」人格上做加法，不破坏合规红线。

**A. 情感锚点组（让用户"舍不得走"）**
- 🌙 **星野夜灯 · 暗色模式**：深蓝 `#0B1426→#16243f` 令牌已就绪，成本极低。晚上打开像抬头看星，浪漫且护眼。
- 🪔 **常驻心灯浮标**：已点灯的用户首页角落有盏呼吸闪烁的小灯，点一下暖光荡开——把"功德"变成可触摸的情感信物。
- 🧚 **星灵小妖精（Mascot IP）**：一个常驻小星灵，负责引导/空状态/彩蛋，给产品一张"脸"。它可以说俏皮话。

**B. 游戏化收集组（把"做善事"变成养星球）**
- ✨ **二十八宿星图**：今日三善/连签变成可点亮的星图，集满一周亮一宿，28 宿集齐解锁隐藏星象——长期进度可视化。
- 🏅 **金色成就徽章**：连签 7 天=「七日星使」、发布 10 篇=「星野记录者」，做成可分享金卡（**顺带产出社媒内容**）。
- 🎴 **每日星签彩蛋**：首页下拉/摇一摇抽一张诗意星签（娱乐向），可变奖励+收集册，零金钱、公示概率。

**C. 微交互彩蛋组（便宜但惊喜感强）**
- 💫 **星屑分型**：赞=流星、收藏=星钉、关注=双星，不同互动不同星象爆发。
- 🫧 **星光漂流瓶**：寄愿墙的愿望写成漂流入银河，别人可"拾起"点亮，双向功德——社交+仪式感。
- 🍂 **节气皮肤**：立春/夏至换主题点缀色与 Hero 文案，制造"每次回来都不一样"。

**D. 首屏冲击力（针对社媒新用户）**
- 🌌 **开场星爆 Hero**：新用户首屏一次星图汇聚动画（尊重 reduced-motion），3 秒内感受到"星野"浪漫，降低跳出。

## 四、想跟你确认的问题（我们共同定）

1. **先攻哪一组？** 情感锚点（夜灯/心灯/星灵）vs 游戏化收集（星图/徽章/星签）vs 微交互彩蛋？资源有限时你更看重"留人"还是"好玩"？
2. **暗色模式做不做？** 令牌现成，半天能出。你担心的是开发量，还是怕浅色金系辨识度被稀释？
3. **金字对比度怎么取舍？** 压深成 `#a9853f` 换可读性（牺牲一点亮金感），还是保留亮金、只把大字/正文改用深金、亮金留给填充与描边？
4. **社媒引流来的新用户**，首屏 3 秒能不能感到"星野"的浪漫？要不要我先做一版更强的开场 Hero 动画给你看？

---
**UI Designer**：像素君
**审查日期**：2026-07-24
**状态**：可协商，未自动改动（除婚礼模板紫渐变 1 行品牌修复）
