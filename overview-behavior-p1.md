# 行为干预方案 · P1 落地概览

> 基于 `行为干预方案.md` P1 阶段，六项良性助推已全部落到真实小程序代码，未改动经济平衡核心、守住合规红线（零金钱、不诱导、不碰迷信敏感词）。

## 落地清单（6 项）

| # | 干预 | 行为原理 | 落地落点 | 关键指标 |
|---|------|----------|----------|----------|
| P1-1 | 连签里程碑 | 进度酬赏 / 序列强化 | `app.js doCheckin` 在连签 3/7/14/21/30 天赠里程碑礼(+2~+8)，写动态+红点；首页飘星、功德中心弹庆祝 | 7日留存、连签天数 |
| P1-2 | 今日三善 | 目标梯度 / 完成满足感 | `app.js getDailyDeeds/markDailyDeed` + 功德中心「今日三善」清单(点灯/随喜/冥想)，每日每事限一次，满3弹庆祝 | 人均日行一善次数 |
| P1-3 | 排行榜跃升 | 社会比较 / 目标可见 | `merit.js buildBoard` 计算「超越 X% 旅人 · 再攒 Y 可前进 N 名」，榜单页展示 | DAU、深度星图解锁率 |
| P1-4 | 共修提醒 | 社会证明 / 损失厌恶 | 功德中心共修页未打卡时展示「今日已有 X 位同伴共修」提醒条 | 共修打卡率 |
| P1-5 | 行为序列消息 | 上下文触发 / 蔡格尼克 | 消息中心按状态生成「星野小提示」（未签到/临近解锁/有星愿/连签） | 唤醒率、关键动作转化 |
| P1-6 | 快捷发布 | 降低摩擦 / 即时反馈 | 首页发现流顶部「随手记一笔」条，直接写发现流+8功德+飘星 | 发布转化率 |

## 改动文件
- `app.js`：新增 `MILESTONES` 里程碑表、`doCheckin` 里程碑礼、连签断签续火保留、每日三善 `getDailyDeeds/markDailyDeed`。
- `utils/merit.js`：新增 `DEEDS`（今日三善清单），随 `ACTIONS/UNLOCKS` 导出。
- `utils/whimsy.js`：文案库补 `milestone/deed/tip`。
- `pages/merit/merit.{js,wxml}`：今日三善卡、排行榜跃升卡、共修提醒条、签到里程碑庆祝；`buildBoard` 返回 `{board, rankNudge}`。
- `pages/message/message.{js,wxml}`：`buildTips` 上下文提示 + 渲染。
- `pages/index/index.{js,wxml}`：快捷发布逻辑 + 输入条、首页签到里程碑飘星。
- `app.wxss`：新增 P1 全部类（浅色暖纸 + 星辉金，统一视觉）。

## 校验
- 6 个 JS 文件 `node --check` 全部通过。
- 跨文件引用 grep 一致：app↔merit/whimsy、merit.js↔wxml、message.js↔wxml、index.js↔wxml、wxss 样式齐备。

## 护栏（与 P0 一致，持续生效）
不造假紧迫、不隐藏退订、不诱导付费；抽奖零金钱且每日上限；功德不可提现/交易/购买；不出现算命改运/医疗承诺；提醒可关。建议上线后监控卸载率/投诉率异常即回滚。

## 下一步（P2）
抽签彩蛋(公示概率)/节气稀缺/时段场景/结缘互惠/损失框架——接口均已具备，可随时推进。
