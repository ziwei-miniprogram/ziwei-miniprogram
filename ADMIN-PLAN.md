# 星野漫游 · 后台管理协调方案（ADMIN-PLAN）

> 状态：规划稿（用户要求"考虑建立"，尚未落地）。技术选型基于本专家栈：Laravel + Livewire + FluxUI。
> 关联：小程序当前 `utils/mock.js` 为纯前端 mock，无真实后端；UGC 仅本地 `utils/censor.js` 预检，无人工复审链路。

## 1. 为什么要建（痛点 → 协调层价值）

| 现状痛点 | 后台协调层解决 |
|---|---|
| UGC 预检只在端上分级，无人工复审 / 申诉队列 | **内容审核台**：censor 命中件进队列，运营复核、误杀申诉 |
| 商城 SKU / 类目 / 折扣 / 限定系列硬编码在 `mall.js` | **商城配置**：可视化上下架、库存、折扣、限定款 |
| 文旅 6 城 / 折扣券 / 限定开售时间写死在 `checkin.js` | **文旅运营**：城市、折扣力度、限定开售时间后台配 |
| 功德账本只在端上 localStorage，无全局视图、无异常监测 | **功德账本**：全局流水、等级分布、刷分/异常预警 |
| 节气 / 二十八宿 / 连签里程碑文案散落多页 | **活动编排**：序列化内容（每日命理流）后台排期 |
| 数据看板缺（DAU / 功德流转 / 转化漏斗） | **数据看板**：运营指标一屏掌控 |

## 2. 技术架构

```
┌─────────────────────┐         HTTPS/JSON API          ┌──────────────────────────┐
│  微信小程序 (前端)    │  ───────────────────────────▶  │  Laravel 11 后端           │
│  (mock.js → 真实API) │  ◀───────────────────────────  │  - API 层 (Sanctum 鉴权)   │
└─────────────────────┘                                │  - 管理后台 (Livewire+FluxUI)│
                                                        └──────────────────────────┘
                                                                   │
                                                            ┌──────┴──────┐
                                                            │  MySQL/PG  │
                                                            └─────────────┘
```

- **后端框架**：Laravel 11（API + Admin 同仓）
- **管理后台 UI**：Livewire 3 + FluxUI（与小程序同源设计令牌：暖纸 `#f7f5f0` / 星辉金 `#c8a35a` / 星夜蓝 `#0B1426`，内置暗色）
- **鉴权**：小程序走 Laravel Sanctum token；后台走 session + RBAC（运营/审核/超级管理员）
- **数据**：MySQL（小程序用户、功德流水、UGC、商品、打卡、活动）

## 3. 后台模块（MVP 优先级）

**P0（最紧迫，先落地）**
- **内容审核台**：UGC（随手记 / 寄愿 / 评论）列表 + censor 命中标记 + 通过/驳回/加白；与端上 `censor.js` 词库同源
- **功德账本**：用户功德流水、等级分布、异常（短时间暴涨）预警；服务端强制"功德虚拟、不可提现"
- **基础 RBAC**：运营 / 审核 / 超管三角色 + 操作审计日志

**P1**
- **商城管理**：SKU、类目、库存、折扣券、限定系列（二十四节气 / 二十八宿 / 城市守护）配置
- **文旅运营**：城市、折扣力度、限定开售时间、GPS 围栏半径
- **数据看板**：DAU、功德流转、转化漏斗（首单 / 客单 / 复购）、文旅打卡进度

**P2**
- **活动编排**：每日命理流（24 节气 × 二十八宿）序列化内容后台排期
- **真实 API 对接**：小程序 `mock.js` 切换为真实 HTTP 调用，端上数据实时化

## 4. 合规红线（延续小程序）

- 服务端再次校验：**功德虚拟、不可交易/提现/购买**；抽奖零金钱 + 概率公示 + 每日上限
- censor 词库后台可维护，与端上预检同源；所有 UGC 留痕可溯
- 商品文案走"祈福/静心/文化/陪伴"，禁"改运/逆天/必应/医疗/付费算命"
- 所有后台操作写审计日志（谁、何时、改了什么）

## 5. 仓库与落地路径建议

- **仓库**：建议独立私有仓 `ziwei-admin`（Laravel），与小程序 `ziwei-miniprogram` 分离，CI 互不干扰；或放 `ziwei-miniprogram/server/` 子目录（monorepo）。推荐**独立仓**。
- **与本专家栈契合**：Livewire + FluxUI 可直接复用小程序设计令牌，后台与小程序视觉同源、开发体验一致。
- **不阻塞小程序**：P0 阶段小程序仍可跑 mock；后台先服务"内容把关 + 数据可视"，再渐进接管 API。

## 6. 下一步（待确认）

1. 确认仓库位置（独立仓 / 子目录）与是否现在脚手架。
2. 确认 P0 范围：先只做「内容审核台 + 功德账本 + RBAC」三件套，还是连同数据看板。
3. 确认小程序 API 化节奏（P2 才接，还是提前）。

> 本文件为规划稿；确认后我可用 Laravel + Livewire + FluxUI 直接脚手架 P0 三件套，并复用小程序设计令牌保证视觉同源。
